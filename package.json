{
  "name": "run-percy-via-module-api",
  "version": "1.0.0",
  "description": "An experiment in running Percy agent via NPM module api instead of CLI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@percy/agent": "0.26.0"
  },
  "devDependencies": {
    "@percy/cypress": "2.3.1",
    "cypress": "4.3.0"
  }
}
